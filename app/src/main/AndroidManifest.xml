<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="alejandro.br.menu">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name="alejandro.br.menu.activities.MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="alejandro.br.menu.activities.SplashActivity">
        </activity>
        <activity android:name="alejandro.br.menu.activities.QRCodeActivity">
        </activity>

        <activity android:name="alejandro.br.menu.activities.DetailMealActivity">
        </activity>
        <activity android:name="alejandro.br.menu.activities.CategoriesActivity">
        </activity>
        <activity android:name="alejandro.br.menu.activities.SecondSplashActivity">
        </activity>
        <activity android:name="alejandro.br.menu.activities.DetailMealTwoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>